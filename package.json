{
  "name": "file-reader-ext",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:chrome": "BROWSER=chrome vite",
    "dev:firefox": "BROWSER=firefox vite",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:web": "tsc && BUILD_TARGET=web vite build",
    "build:chrome": "tsc && BROWSER=chrome vite build",
    "build:firefox": "tsc && BROWSER=firefox vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/webextension-polyfill": "^0.12.5",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "webextension-polyfill": "^0.12.0",
    "xlsx": "^0.18.5"
  }
}